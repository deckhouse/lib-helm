{{- $default := include "helm_lib_affinity_arch_require" (list . (list "arm" "arm64")) }}
resultDefault: {{ $default | toString | quote }}
{{- $custom := include "helm_lib_affinity_arch_require" (list . (list "amd64" "ppc64le")) }}
resultCustom: {{ $custom | toString | quote }}
