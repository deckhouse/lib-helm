suite: helm_lib_module_container_security_context_privileged
templates:
  - helm_lib_module_container_security_context_privileged.yaml
tests:
  - it: default options
    asserts:
      - equal:
          path: securityContext
          value:
            privileged: true