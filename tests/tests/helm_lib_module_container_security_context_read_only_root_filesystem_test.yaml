suite: helm_lib_module_container_security_context_read_only_root_filesystem
templates:
  - helm_lib_module_container_security_context_read_only_root_filesystem.yaml
tests:
  - it: default options
    asserts:
      - equal:
          path: securityContext
          value:
            readOnlyRootFilesystem: true
            allowPrivilegeEscalation: false